# 功能与兼容性边界

0.3 新增按棋子生成候选走法、迭代 alpha-beta、吃子静态搜索、走法排序缓存、取消/节点限制及完整迭代结果回退。Node 宿主提供持续运行的 UCI/基础 UCCI 进程、工作线程停止和 UCI 比赛时钟；规则与搜索仍为 MoonBit 核心。

尚未完成：

- 正式重复局面、长将/长捉裁决、历史计数和完整局面合法性验证。搜索路径重复置 0 只是启发式。
- UCCI 专用时钟、完整协议选项、ponder/searchmoves、开局库、局面分数置换表。
- 更完整的位置评估、棋力对赛/评级、专业引擎速度、大规模搜索与长期稳定性。深度 64 是输入上限，不是保证能达到的搜索深度。
- 独立 GUI 对弈、Windows 之外的进程验收、重负载下严格时钟保证。停止与时间检查存在 64 节点及根走法准备的粒度。

当前新增检查及独立对照见 TESTING.md 和 evidence；旧验证记录保留原日期，不视为新版全量验证。独立仓库与编译入口可本地复现，旧 ZIP/bundle 未重打。本项目未上传或公开发布，不能判定已经达到对标完善度。
